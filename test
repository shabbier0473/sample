pipeline{
    agent { label 'maven' }
    stages{
        stage('test'){
            tools { maven "MAVEN_HOME" }
            steps{
                sh 'cd /home/maven/workspace/mainpipeline'
                sh 'mvn test'
            }
        }
    }
}
