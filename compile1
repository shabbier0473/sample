pipeline{
    agent { label 'maven' }
    stages{
        stage('compile'){
            tools { maven "MAVEN_HOME" }
            steps{
                sh 'cd /home/maven/workspace/mainpipeline'
                sh 'mvn compile'
            }
        }
    }
}
