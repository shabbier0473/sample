pipeline{
    agent { label 'maven'
        customWorkspace '/home/maven/workspace/mainpipeline'
    }
    stages{
        stage('compile'){
            tools { maven "MAVEN_HOME" }
            steps{
                sh 'mvn compile'
            }
        }
    }
}
