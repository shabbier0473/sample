pipeline{
    agent { label 'maven' }
    stages{
        stage('compile'){
            tools { maven "MAVEN_HOME" }
            steps{
                sh 'mvn compile'
            }
        }
    }
}
