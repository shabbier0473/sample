pipeline{
    agent any{ 
        customWorkspace '/var/lib/jenkins/workspace/mainpipeline'
    }
    stages{
        stage('compile'){
            tools { maven "MAVEN_HOME" }
            steps{
                sh 'mvn compile'
            }
        }
    }
}
